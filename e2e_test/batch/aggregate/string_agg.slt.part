statement ok
SET RW_IMPLICIT_FLUSH TO true;

statement ok
create table t(v1 varchar(5));

statement ok
insert into t values ('foo');

query T
select string_agg(v1) from t;
----
foo

statement ok
insert into t values ('bar'), ('baz');

query T
select string_agg(v1) from t;
----
foobarbaz

statement ok
drop table t
